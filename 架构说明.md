# 模型转换服务架构说明

## 边界

现有 Java 平台继续负责摄影测量业务、MinIO 成果归档、验收与发布；本服务只负责确定性的模型转换任务。Java 不拼接 Obj2Tiles 命令，也不感知 Fast Quadric 或 meshoptimizer 的实现细节。

```text
NodeODM geo.obj + reference_lla.json
                  ↓
        CLI convert ────────┐
                            ├── ConversionRunner（唯一转换边界）
        HTTP 作业 Worker ───┘        │
                  ↓                  ├── IModelConverter（Obj2Tiles 定制实现）
        持久任务状态/队列            ├── ITilesetValidator（输出校验）
                  ↓                  └── 暂存 → 校验 → 原子发布
        serve / 结果路由 ←── 已发布 tileset
```

`ConversionRunner` 是应用层唯一负责“一次转换从暂存到发布”完整事务的模块：暂存目录准备、Obj2Tiles 进程调用、超时/取消、日志、tileset 校验、输出冲突拒绝与原子发布，产出可区分的结构化结果（成功/输出冲突/转换失败/校验失败/超时/取消）。CLI（`ConvertCommand`）与 HTTP 后台 Worker 都只是它的适配器，不各自复制流程。

`IModelConverter` 是外部转换算法接缝，当前实现调用定制 Obj2Tiles 可执行文件；`ITilesetValidator` 是校验接缝。长期的算法演进在该接缝后新增实现，不改变任务 API 与 CLI。

`ConversionProgressRegistry` 接收转换核心和 Obj2Tiles 输出解析器上报的单调进度。CLI 将进度打印到控制台；HTTP 作业通过详情、`/status` 和 `/progress` 查询。Obj2Tiles 原始 stdout/stderr 同时进入作业日志与服务控制台，便于容器日志排障。

转换参数使用统一强类型模型：`ConversionSettingsResolver` 把配置档（profile）与白名单覆盖（`ConversionOverrides`）合并成不可变快照 `ConversionSettings`；`Obj2TilesCommandBuilder` 只接受快照，不接触 HTTP DTO 或命令行对象。

## 长期稳定性设计

- 转换参数来自服务端白名单配置档与强类型覆盖，调用方不能注入任意命令参数；
- `ProcessStartInfo.ArgumentList` 直接传参，不经过 Shell；
- 默认只运行一个重型任务，避免多个摄影测量模型同时耗尽内存；
- 状态文件在同目录原子替换，服务重启会恢复 `Running/Validating` 任务；
- 作业持久化完整参数快照，恢复和重试读取快照，不因 profile 后续修改漂移；每次重试创建新任务，旧失败记录保留用于审计；
- API 输出目录可指定为 `OutputRoot` 内相对路径，CLI 使用任意本机路径；路径边界只在适配层处理；
- `tileset.json`、外部 tileset、内容 URI、`geometricError` 和 `REPLACE` 在发布前校验；
- 最终输出存在时拒绝覆盖；API 对同一成功作业的既有输出只做幂等复核；
- 结果文件路由以“成功作业 + 该作业已发布输出根”建立授权，不暴露整个 `OutputRoot`、暂存或状态目录；
- 转换超时或取消时终止完整子进程树；
- [2026-09-07] 转换前输入准备固定为两步：超大纹理流式规范化（`TextureNormalizationPipeline`）→ 深层封底剥离（`DeepBottomGeometryFilter`，默认开启、`--strip-deep-bottom false` 可关）；两者都在任务专属副本上改写，源输入全程只读，副本在发布前删除；
- HTTP 默认开放无凭据的任意来源 CORS，支持独立部署的 Mars3D/Cesium 前端直接加载结果；
- HLOD 的全局 wrapper 不占用 `lods`，例如 `lods=5` 输出 LOD-0 至 LOD-4；
- 上游 Obj2Tiles 源码随仓库固定，任何升级必须重新跑全套验证。

## 当前算法结论

近期方案可行，但“最低 50%”只是基于当前粮库/道路样本的安全阈值，不等于几何误差证明。它解决固定比例过度减面的已知问题，不能替代真实误差驱动 HLOD。

长期实现顺序：

1. 收集固定相机位、三角面数、纹理体积、GPU 内存和构建耗时基线；
2. 将 meshoptimizer 封装成独立转换器，先做单瓦片 absolute-error 简化和边界锁定；
3. 实现叶子空间分块、父节点合并与 bottom-up HLOD；
4. 把每级真实误差写入 `geometricError`，并加入裂缝与视觉回归；
5. 达到同数据集门槛后再替换生产配置档。

## 许可

Obj2Tiles 为 AGPL-3.0，meshoptimizer 为 MIT。当前镜像包含定制 Obj2Tiles，因此上线前必须完成 AGPL 源码提供义务评估；不能把“独立进程”误认为自动规避许可义务。
