# 使用教程：从 OBJ 模型到 3D Tiles

本教程面向第一次使用本服务的人，用最短的路径跑通一次完整转换。三种用法按场景任选其一，命令均可在 PowerShell 中直接复制执行；更多参数与细节请见 [README](./README.md)。

| 你的场景 | 适合路线 | 大致耗时 |
|---|---|---|
| 手里有一个 OBJ 文件夹，转出 3D Tiles 就行 | 路线 A：一条命令本地转换 | 模型几分钟内 |
| 想在机器上跑常驻服务，通过网页/平台/接口提交转换 | 路线 B：本机 HTTP 服务 | 首次启动 1~2 分钟 |
| 模型和算力都在远程 Linux 服务器上，人在 Windows 办公机 | 路线 C：远程服务器使用 | 同 B，多一步隧道 |

## 第 0 步：准备

只需安装 Docker（含 Docker Compose），转换所需的 Obj2Tiles 等工具已全部内置于镜像，无需其他依赖。

```powershell
docker pull dz98/obj-to-3dtiles-service:v3.1
```

模型包要求：**一个 `.obj` 文件 + 同目录的 `.mtl` 和纹理文件**（ODM 产物 `odm_textured_model_geo.obj` 最佳）。ZIP 中允许有外层目录，但只能有一个 `.obj`。

## 路线 A：一条命令本地转换（CLI）

适合一次性转换、脚本和流水线。把宿主机目录挂进容器，在容器内用 Linux 路径执行 `convert`：

```powershell
docker run --rm `
  -v "C:/Users/qkp/Desktop:/data/input:ro" `
  -v "D:/tiles:/data/output" `
  dz98/obj-to-3dtiles-service:v3.1 `
  convert --input /data/input/obj2 --output /data/output/obj2 `
  --lat 33.62678591 --lon 117.00336389 --alt 0 --lods 4
```

要点：

- 容器内参数必须用 `/`（如 `--input /data/input/obj2`）；`-v` 冒号左侧的宿主机路径随意；
- `--lat/--lon/--alt` 提供地理参考；只是本地测试几何时可换成 `--local`；已有 `reference_lla.json` 时用 `--reference-lla /data/input/obj2/reference_lla.json`。三者互斥；
- 退出码直接反映成败（`0` 成功、`2` 参数/坐标缺失、`3` 输出目录已存在），可在流水线中直接判断；
- 转换进行中可另开窗口看日志：`Get-Content D:\tiles\obj2.log -Wait -Tail 20`。

Linux 上用 `docker run` 时，宿主机输出目录需预先存在并对容器 UID 1654 可写。

## 路线 B：本机常驻 HTTP 服务

适合反复提交转换、给前端或平台当后端。使用 Compose 启动：

```powershell
docker compose pull
docker compose up -d
```

启动后监听 `http://127.0.0.1:8091`。数据默认落在：

- Windows：`%USERPROFILE%\obj-to-3dtiles-service-data`（下分 `input/output/state`）；
- Linux：`/var/lib/obj-to-3dtiles-service-data`；
- 想换磁盘：`$env:OBJ_TO_3DTILES_DATA_ROOT = "E:/model-data"` 后再 `docker compose up -d`。

`docker compose ps` 里 `volume-init` 显示 `Exited (0)` 属正常（它是初始化目录权限的一次性容器）。

### B-1 确认服务就绪

```powershell
Invoke-RestMethod http://127.0.0.1:8091/health/ready
```

预期返回 `{"status":"ready"}`。

### B-2 打包并上传模型

```powershell
$zip = "$env:TEMP\obj1.zip"
Compress-Archive -Path "D:\models\obj1\*" -DestinationPath $zip
$upload = curl.exe -s -X POST http://127.0.0.1:8091/api/v1/uploads `
  -H "Content-Type: application/zip" --data-binary "@$zip" | ConvertFrom-Json
$upload
```

保存响应里的 `inputPath`，下一步直接使用（它是服务内部相对路径，不要自己拼绝对路径）。若包内有 `reference_lla.json`，响应会附带 `referenceLlaPath`。

### B-3 创建转换

已知真实坐标的正式请求：

```powershell
$request = @{
    inputPath    = $upload.inputPath
    profile      = "industrial-jpeg"
    geoReference = @{
        latitude  = 33.62678591
        longitude = 117.00336389
        altitude  = 0
    }
    overrides    = @{ lods = 5 }
}

$job = Invoke-RestMethod -Method Post `
    -Uri "http://127.0.0.1:8091/api/v1/conversions" `
    -ContentType "application/json" `
    -Body ($request | ConvertTo-Json -Depth 8)
$id = $job.id
Write-Host "转换 ID: $id"
```

只想先验证模型结构（无坐标）：把 `profile` 换成 `"local-test"`，并可加 `overrides = @{ lods = 2; maxTextureSize = 2048 }` 快速冒烟。若上传响应带 `referenceLlaPath`，用 `referenceLlaPath = $upload.referenceLlaPath` 替代 `geoReference`。

### B-4 查询进度直到结束

```powershell
do {
    Start-Sleep -Seconds 3
    $status = Invoke-RestMethod "http://127.0.0.1:8091/api/v1/conversions/$id/status"
    Write-Host "$($status.progress.percent)%  $($status.state)  $($status.progress.message)"
}
until ($status.state -in @("Succeeded", "Failed", "Canceled"))
```

关闭窗口或断开连接不会取消已提交的转换；重连后用同一个 `$id` 继续查即可。

### B-5 取结果

```powershell
# 文件清单（含每个文件的 SHA-256）
$result = Invoke-RestMethod "http://127.0.0.1:8091/api/v1/conversions/$id/result"

# 下载完整结果 ZIP 并解压
$downloadDir = "$env:USERPROFILE\Downloads\obj-to-3dtiles-$id"
New-Item -ItemType Directory $downloadDir -Force | Out-Null
Invoke-WebRequest "http://127.0.0.1:8091/api/v1/conversions/$id/result.zip" `
    -OutFile "$downloadDir\result.zip"
Expand-Archive "$downloadDir\result.zip" -DestinationPath "$downloadDir\tiles"
```

也可以不经下载，直接把瓦片入口给前端加载：
`http://127.0.0.1:8091/api/v1/conversions/$id/result/tileset.json`。

注意：终态作业产物默认 **24 小时后自动清理**，请及时归档，不要把服务 URL 当长期成果地址。

## 路线 C：远程服务器使用（SSH 隧道）

服务不暴露公网，Windows 侧先建隧道（窗口 1 保持运行）：

```powershell
ssh -N -L 18091:127.0.0.1:8091 root@<服务器IP>
```

之后把路线 B 中所有 `http://127.0.0.1:8091` 换成 `http://127.0.0.1:18091`，其余步骤完全相同。服务器侧用同样的 `docker compose pull && docker compose up -d` 启动。

## 坐标模式与 LOD 速查

三选一，互斥：

| 场景 | CLI | API |
|---|---|---|
| 有 `reference_lla.json` | `--reference-lla <路径>` | `referenceLlaPath` |
| 已知经纬高 | `--lat/--lon/--alt` | `geoReference` |
| 本地测试，无坐标 | `--local` | `profile = "local-test"` |

LOD 层数默认 5 层（`LOD-0`～`LOD-4`），空间优先四叉树每层瓦片数约 ×4：

| `lods` | 用途 |
|---:|---|
| 2 | 快速冒烟 |
| 4 | 较快的效果验证 |
| 5（默认） | 正式转换 |

## 在 Cesium / Mars3D 中加载

- 直接把 `tilesetUrl`（`…/result/tileset.json`）交给 `Cesium3DTileset` / Mars3D 的 TilesetLayer；
- 预览建议：`maximumScreenSpaceError=2`、`cullWithChildrenBounds=false`；
- 服务默认开放 CORS，其他域名可直接调用；生产环境请在网关层做认证与来源限制；
- ODM 的 OBJ 为 Z-up，地理参考输出在 Cesium 中会"竖立"，需要在查看侧对根 transform 叠加 `Rx(-90°)` 修正（详见 README「前端查看器接入」）。

## 高频问题速查

- **`docker run` 报 `invalid reference format`**：在 cmd 里用了 PowerShell 的反引号续行。cmd 用 `^` 或写成一行。
- **容器内路径报错**：`-v` 冒号右侧和 `--input` 必须是 `/` 开头的 Linux 路径。
- **创建作业报"输入文件不存在"**：`inputPath` 要用上传接口返回的值，不要自己拼宿主机路径。
- **查询进度返回 405**：进度/状态用 GET 且不带请求体；POST 只用于创建、取消、重试。
- **输出目录已存在（退出码 3 / 409）**：换目录名或人工删除旧目录，服务故意不提供覆盖。
- **玩具级小模型转换失败**：极小网格是上游已知边界，测试时关闭 HLOD（`overrides.hierarchical = false` 或 CLI `--hierarchical false`）。

## 下一步阅读

- [README](./README.md)：完整参考（CLI 全参数、HTTP API 全接口、配置项、离线构建、FAQ 全集）；
- [架构说明](./架构说明.md)：转换管线与 HLOD 设计；
- [项目说明](./项目说明.md)：项目定位与许可。
